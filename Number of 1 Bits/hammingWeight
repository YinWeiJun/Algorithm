class Solution(object):
    def hammingWeight(self, n):
        """
        :type n: int
        :rtype: int
        """
        # s = str(bin(n))
        # s = s[2:]
        # return len(s.replace("0",""))

        return bin(n).count("1")
