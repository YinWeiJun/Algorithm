Given two integers n and k, return all possible combinations of k numbers out of 1 ... n.

Example:

Input: n = 4, k = 2
Output:
[
  [2,4],
  [3,4],
  [2,3],
  [1,2],
  [1,3],
  [1,4],
]

即：给定n为取值范围（1,2,3……n）,k为取值个数，结果要求为，取值范围内取k个数，且递增排列，不重复
